# Environment — never commit credentials
.env
.env.*
!.env.example

# DB backups produced by scripts/backup-db.sh — kept local, never pushed
# (dumps contain full customer data — treat as sensitive).
backups/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel


# Binary artefacts — don't commit
*.zip
*.tar.gz
